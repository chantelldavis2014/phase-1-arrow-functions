// const divide = function(num1,num2){
//    return(num1 / num2)
// }
// divide(2000 / 100)

// const square = (a) => (a * a)

// const add = (b,r) => (b + r)

const divide = function(a,b){
   return (a / b);
}

const square = (a) => (a * a);


const add = (c,d) => (c + d);









